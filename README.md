# Test Docker Retag

E2E test repo for `docker-build-retag` workflow in buildon-github-actions.

Tests file-pattern-match versioning (version extracted from Dockerfile ENV KUBECTL_VERSION). Retags alpine:3.21 from Docker Hub.
